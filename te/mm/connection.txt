#Modify this file as necessary, but be careful of whitespace; "settings.py" isn't very smart.

#The IP the client will try to connect to.  If you're just running locally, `"localhost"` will make
#	the client connect to a server running on the same computer.  For a computer running remotely,
#	set this to be that server's public IP address (NAT punchthrough is not supported).
client_ip = "localhost"
#client_ip = "123.123.123.123"

#Tell the server to listen to all IPs.
server_ip = "0.0.0.0"

#The port used for both the client and the server
#port = 6317
port = 20298